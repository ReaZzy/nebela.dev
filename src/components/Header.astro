---
import { ThemeToggle } from "@/components/ThemeToggle";

interface Props {
  currentPage?: "home" | "blog";
}

const { currentPage } = Astro.props;
---

<header
  class="w-full max-w-4xl mx-auto px-4 mt-8 mb-4"
  transition:name="header"
  transition:animate="fade"
>
  <nav class="flex gap-4 font-mono text-base items-center" aria-label="Main navigation">
    <a
      href="/"
      {...currentPage !== "home" && { "data-astro-navigate": true }}
      class={currentPage === "home"
        ? "text-green-600 dark:text-green-400 cursor-default pointer-events-none"
        : "text-zinc-500 dark:text-zinc-400 hover:text-green-600 dark:hover:text-green-400 active:text-green-600 dark:active:text-green-400 transition-colors duration-200"}
      {...currentPage === "home" && { "aria-current": "page" }}
    >
      [h] home
    </a>
    <a
      href="/blog"
      {...currentPage !== "blog" && { "data-astro-navigate": true }}
      class={currentPage === "blog"
        ? "text-green-600 dark:text-green-400 cursor-default pointer-events-none"
        : "text-zinc-500 dark:text-zinc-400 hover:text-green-600 dark:hover:text-green-400 active:text-green-600 dark:active:text-green-400 transition-colors duration-200"}
      {...currentPage === "blog" && { "aria-current": "page" }}
    >
      [b] blog
    </a>
    <ThemeToggle client:load className="ml-auto" />
  </nav>
</header>
